/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLongestSubstring = function (s) {
  let longestArray = [];
  for (let i = 0; i < s.length; i++) {
    if (longestArray.includes(s[i])) {
    }
  }
};
